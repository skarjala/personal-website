# AGENTS.md

Project-specific instructions for `/Users/sandeepkarjala/Downloads/personal-website`.

## Autonomy

For this project, do not ask for permission before doing ordinary local development work needed to run, inspect, debug, or verify the website.

Pre-approved local actions include:

- Starting, stopping, or restarting the local dev server.
- Running `npm install`, `npm run dev`, `npm run build`, and similar project-local npm commands.
- Using Browser Use and Computer Use to open, inspect, click through, screenshot, and visually verify the local site.
- Editing project files to implement requested website changes.
- Reading public reference sites and public project links provided by the user.
- Iterating on visual/design fixes until the local site looks and interacts correctly.

## Boundaries

Still ask before actions that are destructive, external, or account-affecting:

- Deploying the site or changing hosting/domain settings.
- Sending messages, submitting forms, or posting content.
- Uploading files to third-party services.
- Deleting local or cloud data.
- Changing sharing permissions or account settings.
- Handling credentials, payment details, API keys, or other sensitive data.

## Workflow

- Prefer simple static-site changes unless the user asks for a framework or backend.
- Keep the design close to the current site aesthetic: compact, dark, resume-like, minimal, and interaction-polished.
- Verify changes locally with Browser Use after meaningful frontend edits.
- Use Computer Use as an additional visual check when the user asks for browser/window-level verification.
- Keep changes scoped to the requested website behavior or content.
